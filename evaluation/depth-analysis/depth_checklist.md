# Depth Checklist

Score each indicator as `1` (present) or `0` (missing).

- Gives reasoning (not just final answer)
- Includes example(s)
- Includes trade-offs or alternatives

You can also assign a 1-5 depth rating in `depth_score_1_to_5`.
